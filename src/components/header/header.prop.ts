// @ts-ignore
import PropTypes from "prop-types";

const headerDefaultProps = {
  siteTitle: ``,
}

const headerPropTypes = {
  siteTitle: PropTypes.string,
}

export { headerDefaultProps, headerPropTypes}
